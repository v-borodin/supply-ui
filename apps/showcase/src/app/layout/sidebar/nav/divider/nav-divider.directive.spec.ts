import { NavDividerDirective } from './nav-divider.directive';

describe('NavDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new NavDividerDirective();
    expect(directive).toBeTruthy();
  });
});
